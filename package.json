{
  "author": "Asok P",
  "name": "fetch-mailchimp-fields",
  "description": "Lookup a Subscriber in Mailchimp List and get their merge field details",
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.2.0",
    "cssnano": "^4.1.10",
    "tailwindcss": "^0.7.4",
    "tailwindcss-tables": "^0.2.0",
    "watch": "^1.0.2"
  },
  "scripts": {
    "build:css": "postcss public/css/src/tailwind.css --output public/css/tailwind.css",
    "watch": "watch 'npm run build:css' ./public"
  }
}
